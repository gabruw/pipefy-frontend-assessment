//#region Imports

import { makeStyles } from '@material-ui/core/styles';

//#endregion

const useStyles = makeStyles({
    card: {
        margin: 5,
        height: 250
    }
});

export default useStyles;
