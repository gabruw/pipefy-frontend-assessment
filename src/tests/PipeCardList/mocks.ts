//#region Imports

import { Organizations } from 'models/organization';
import { PIPE_MOCK } from 'tests/PipeCard/mocks';

//#endregion

export const ORGANIZATIONS_MOCK: Organizations = {
    organization: {
        pipes: [PIPE_MOCK]
    }
};
